[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node openclaw.mjs gateway --bind lan --port ${PORT:-8080} --allow-unconfigured"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
