[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node openclaw.mjs gateway --bind lan --port $PORT --allow-unconfigured"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
